pipeline {
    agent any
    tools { 
        Maven 'Maven 3.6.0' 
        jdk 'Open JDK 11' 
    }
    stages {
        stage ('Initialize') {
            steps {
                    echo "PATH = ${PATH}"
                    echo "M2_HOME = ${M2_HOME}"
             }
        }

        stage ('Build') {
            steps {
                """
                mvn test
                """
            }
        }
    }
}
